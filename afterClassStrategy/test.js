//Mecanismo notificacion

//ENVIAR NOTIFICACION

// Strategy Pattern

//Correo Sendgrid, gmail Cada implementacion puede o va a ser diferente
//Push firebase
//SMS twilio
//Whatsapp twilio
//Escalable


if (notifacion = 'Correo') {
   //Implementacion de envio de notificacion via correo
}

if (notifacion = 'Push') {
    //Implementacion de envio de notificacion via Push
 }

 if (notifacion = 'SMS') {
    //Implementacion de envio de notificacion via SMS
 }

 if (notifacion = 'Whatsapp') {
    //Implementacion de envio de notificacion via Whatsapp
 }

if (notificacion = 'Nueva') {
    //Implementacion
}